{
  "name": "threecountry-srpg-branch",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm --prefix srpg run check",
    "test": "npm --prefix srpg test",
    "build": "npm --prefix srpg run build"
  }
}
