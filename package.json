{
  "name": "threecountry-chronicle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && node scripts/build.mjs",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs",
    "preview": "python3 -m http.server 4173 -d dist"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
